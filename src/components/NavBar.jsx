import React from 'react'
import { Box, AppBar, Toolbar } from '@mui/material'

export const NavBar = () => {
  return (
    <AppBar>
        <Toolbar>
            NavBar
        </Toolbar>
    </AppBar>
  )
}
